# CashUp Agent - Master Requirements File
# Complete dependencies for EABL demo system

# Core FastAPI & Web
fastapi==0.115.14
uvicorn[standard]==0.29.0
python-multipart>=0.0.18
pydantic>=2.0,<3.0
pydantic_settings>=2.0,<3.0

# Database & Caching  
asyncpg>=0.28.0
redis>=4.0.0

# Monitoring & Metrics
prometheus-client>=0.20.0

# ML & Document Processing
onnxruntime>=1.15.0,<2.0
numpy>=1.24.0,<2.0
tokenizers>=0.20.0,<1.0
huggingface_hub>=0.20.0,<1.0

# Azure Services (for production tier 3)
azure-ai-formrecognizer>=3.3.0
azure-core>=1.26.0  
azure-identity>=1.12.0

# HTTP & Networking
httpx>=0.24.0

# Utilities
python-dotenv>=1.0.0
regex>=2023.0.0
typing-extensions>=4.5.0

# Development & Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# EXPLICITLY EXCLUDED (for performance)
# torch - Excluded (using ONNX instead)
# transformers[torch] - Excluded (using tokenizers only)
# tensorflow - Excluded